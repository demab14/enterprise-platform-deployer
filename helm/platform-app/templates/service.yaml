apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "platform-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "platform-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
